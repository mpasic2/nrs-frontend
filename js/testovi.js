let assert = chai.assert;

describe('#inkrement', function() {
	it('treba vratiti minimalnu vrijednost kolicine', function() {
		assert.equal(pov(), 1);
	});
});

describe('#dekrement', function() {
	it('treba vratiti minimalnu vrijednost kolicine', function() {
		assert.equal(pov(), 1);
	});
});

/*describe('saberi()', function() {
	it('saberi dva broja', function(){
		assert.equal(pov(), 4);
	});
});*/
